# logrotate 조각 파일. 설치 예:
#   sudo cp deploy/logrotate.conf /etc/logrotate.d/quant_trader

/home/ubuntu/quant_trader/logs/*.log {
  daily
  rotate 30
  compress
  missingok
  notifempty
  copytruncate
}
